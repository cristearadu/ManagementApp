<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examplee.radu.managementapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ticket.ReceiveTicket"></activity>
        <activity android:name=".Ticket.SendTicket" />
        <activity android:name=".User.EditDetails" />
        <activity android:name=".ChatActivity.FindUserActivity" />
        <activity android:name=".ChatActivity.Chat.ChatListAdapter" />
        <activity android:name=".ChatActivity.MainChatActivity" />
        <activity android:name=".ChatActivity.ChatActivity" />
        <activity android:name=".User.EditUser" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".LoginAndSignUp.SignUp" />
        <activity android:name=".LoginAndSignUp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>